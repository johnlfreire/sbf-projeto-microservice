spring.application.name=sfb-user
spring.cloud.config.uri=http://localhost:8888

# Permite que o actuator possa atualizar as configurações em tempo de execução 
management.endpoints.web.exposure.include=*